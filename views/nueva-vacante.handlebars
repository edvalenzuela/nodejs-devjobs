<main class="contenedor">
  <form action="/vacantes/nueva" method="POST" class="default-form">
  
    <h3>Información general</h3>
    <div class="campo">
      <label for="">Titulo</label>
      <input type="text" name="titulo" placeholder="Ej: React developer" required>
    </div>
    <div class="campo">
      <label for="">Empresa</label>
      <input type="text" name="empresa" placeholder="Empresa contrata" required>
    </div>
    <div class="campo">
      <label for="">Ubicación</label>
      <input type="text" name="ubicacion" placeholder="Ej: Chile o Remoto" required>
    </div>
    <div class="campo">
      <label for="">Salario (USD)</label>
      <input type="text" name="salario" placeholder="Ej: $2500 USD" required>
    </div>
    <div class="campo">
      <label>Contrato</label>
      <select name="contrato">
        <option value="" disabled selected>-- Selecciona --</option>
        <option value="freelance">Freelance</option>
        <option value="tiempo completo">Tiempo completo</option>
        <option value="medio tiempo">Medio tiempo</option>
        <option value="por proyecto">Por proyecto</option>
      </select>
    </div>

    <h3>Descripción del puesto</h3>
    <div class="campo descripcion">
      <label for="">Descripción</label>
      <input type="hidden" id="x" name="descripcion">
      <trix-editor input="x"></trix-editor>
    </div>

    <h3>Conocimientos</h3>
    <ul class="lista-conocimientos">
      {{#seleccionarSkills a}}
      {{/seleccionarSkills}}
    </ul>

    <div class="campo centrar-horizontal">
      <input type="hidden" name="skills" id="skills">
      <input type="submit" value="publicar" class="btn btn-azul">
    </div>
  </form>
</main>